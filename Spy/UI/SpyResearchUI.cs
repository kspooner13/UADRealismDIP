// Spy Tech is now integrated into the game's research system (G.GameData.technologies).
// Spy technologies are injected at load via SpyTechInjection and appear in the standard Research window.
// This file is kept for any future Research-window-specific hooks (e.g. tooltips).

namespace Spy
{
    public static class SpyResearchUI
    {
        // No custom panel; spy techs use the standard research UI.
    }
}
